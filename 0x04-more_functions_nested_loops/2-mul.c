#include "main.h"

/**
 * mul - finds the product of 2 integeres
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int prod = a * b;

	return (prod);
}
